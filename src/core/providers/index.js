/** Re-export shared providers — prefer importing from here going forward. */
export { AuthProvider, useAuth } from '../../shared/providers/AuthProvider.jsx';
export { ClientProvider, useClient } from '../../shared/providers/ClientProvider.jsx';
