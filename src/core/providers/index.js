/** Re-export shared providers — prefer importing from here going forward. */
export { AuthProvider, useAuth } from '../../context/AuthContext.jsx';
export { ClientProvider, useClient } from '../../context/ClientContext.jsx';
