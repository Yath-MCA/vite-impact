# constants

## Purpose / ownership
Shared constant values (non-session). Session constants live in `services/session`.

## Key files
- `constant modules`

## Dependencies
Imported by UI/services.

## Status
**active**
