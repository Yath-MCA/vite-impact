# api

## Purpose / ownership
REST transport (`apiService`), endpoints, headers, form-encoded `jsondata` posts.

## Key files
- `apiService.js`
- `shareInviteClient.js`

## Dependencies
axios; `window.ENV` / VITE_*; used app-wide.

## Status
**active**
