# reports

## Purpose / ownership
Shared report UI building blocks (filters, charts, grid). Screen-level reports live under `src/features/reports`.

## Key files
- `ReportFilters.jsx`
- `ReportCharts.jsx`
- `ReportGrid.jsx`

## Dependencies
charts/grid; feature migration target `features/reports`.

## Status
**active · migrate-to-feature**
