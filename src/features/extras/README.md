# extras

## Purpose / ownership
Legacy/alternate dashboard variants. Quarantine — prefer `features/dashboard` and client routes cleanup.

## Key files
- `AdminDashboard.jsx`
- `ClientDashboard.jsx`
- `DashboardPage.jsx`
- `DocDashboard.jsx`
- `DocFinderDashboard.jsx`

## Dependencies
Some imports still used (e.g. `/client` → ClientDashboard). Fix imports before deletion.

## Status
**quarantine / import-debt**
