# collection-config

## Purpose / ownership
TS collection registry/resolver (newer config path parallel to config/collections).

## Key files
- `registry/resolver modules`

## Dependencies
May overlap `src/config`; consolidate carefully.

## Status
**active · parallel to config**
