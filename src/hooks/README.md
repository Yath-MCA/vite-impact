# hooks

## Purpose / ownership
Shared React hooks (cross-feature). Feature-specific hooks live under `features/<name>/hooks`.

## Key files
- `index.js and hook modules`

## Dependencies
React; browsers storage helpers as needed.

## Status
**active**
