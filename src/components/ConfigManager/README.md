# ConfigManager

## Purpose / ownership
Deprecated thin re-exports for Config Manager. Canonical implementation: `src/features/config-manager/`.

## Key files
- `ConfigManagerPage.jsx (re-export)`
- `legacy siblings may still exist — prefer features path`

## Dependencies
Re-exports feature module; do not grow logic here.

## Status
**thin re-export**
