Prompt 14 — AG-Grid Enterprise Integration

Act as a Senior React Data Grid Engineer.

Integrate AG-Grid Enterprise (paid version) into the CMS platform.

Requirements:

Install:

ag-grid-community
ag-grid-enterprise
ag-grid-react

Enable enterprise features:

row grouping
pivoting
aggregation
excel export
column tool panel
filters
server-side row model

Create reusable grid wrapper:

src/components/Grid/AgGridWrapper.jsx

Features:

column definitions
dynamic filtering
pagination
server side data
custom cell renderers

Ensure grids support large dataset performance.